REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=${REDIS_PASSWORD}

POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

AUTH_ENABLED=true
AUTH_HEADERS=X-Auth-Token
AUTH_TOKENS=${JUDGE0_TOKEN}

# Keep this conservative for MVP workloads
CPU_TIME_LIMIT=5
WALL_TIME_LIMIT=8
MEMORY_LIMIT=128000

# You can prune languages post-setup if needed.
